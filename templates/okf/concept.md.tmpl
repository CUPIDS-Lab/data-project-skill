---
type: <Concept-type>
title: <Human-readable name>
description: <One sentence on what this concept is and why it exists.>
resource: <canonical URI for this concept, e.g. a dataset landing page or table URL>
tags: [documented, transparent]
timestamp: {{YEAR}}-01-01T00:00:00Z
---

<!-- Generic OKF concept template. The ONLY required frontmatter field is `type` (a non-empty string such as "Dataset" or "Table"); everything else above (title, description, resource, tags, timestamp) is optional — keep what helps a reader, delete what you cannot fill in honestly. Group concepts in subdirectories by type (datasets/, tables/). Cross-links use bundle-relative paths beginning with `/` (rooted at the bundle's index.md), and the body sections below are conventional, not mandatory — right-size to the concept. -->

Open with a short prose summary of this concept: what it represents, its grain or scope, and why it matters to someone deciding whether to reuse it. Link related concepts with bundle-relative paths, e.g. this concept is part of [the {{PROJECT_SLUG}} dataset](/datasets/{{PROJECT_SLUG}}.md).

# Schema

Describe the structure — fields, types, and meanings — or, for a table concept, render it as a `Column | Type | Description` markdown table. Document the schema here rather than duplicating it across concepts, and point at the field-level `DATA-DICTIONARY.md` as the authority.

# Examples

Show one or two concrete, representative records or values so a reader can picture the data without loading it.

# Citations

How to cite this concept and the upstream sources it derives from. {{PI_NAME}} et al., *{{PROJECT_NAME}}*, {{LAB_NAME}}, {{YEAR}}.
