---
name: data-intake
description: >-
  Read before acquiring, assessing, or first understanding any dataset in {{PROJECT_NAME}}. Covers recording
  provenance and license on ingestion, verifying that the source is reachable and that you have the authority
  to use it, never editing raw data in place, and securing the subject-matter knowledge needed to read the data
  correctly. Trigger on "ingest," "import data," "new source," "where did this come from," or "can we use this."
---

# Data intake

Acquire, assess, and understand a dataset before it becomes a dependency. The work here is what makes everything downstream trustworthy, so do not rush it to get to analysis.

Record provenance and license at the moment of ingestion, not later from memory. For every source, capture where it came from, how and when you obtained it, the chain of custody, and the license or terms that govern its reuse, and write those into `DATA-DICTIONARY.md` (per-field provenance) and `decision-log.md` (why this source). A dataset whose origin you cannot reconstruct is a liability, not an asset.

Verify availability and authority before you build on the data. Confirm the source is reachable and re-releasable so the pipeline can regenerate results from it later, and confirm you actually have the right to use, transform, and share it under its license and any applicable terms — including privacy law and, where Indigenous data is implicated, the CARE Principles. If the right to use is unclear, resolve that question before ingesting rather than after you have built on the data.

Never edit raw data in place. Treat everything under `data/raw/` as immutable: read it, write derived data to `data/interim/` or `data/processed/`, and make sure any derived output can be regenerated from the original. Editing the source destroys your ability to audit against it and to reproduce your own work.

Learn or recruit the knowledge needed to interpret the data. Numbers without context get misread, so identify the subject-matter expertise a dataset requires and either learn it or bring in the person who holds it before drawing conclusions. The people who collected or own the data hold the context that prevents misreadings; stay in contact with them, and record what you learn so the next reader does not have to rediscover it.

<!-- IF:SENSITIVE -->This project handles **{{SENSITIVITY_TIER}}** data. Before ingesting anything that touches people, confirm the consent and legal basis cover this use, take only what the stated purpose needs, route the data into the controlled tier defined in `GOVERNANCE.md`, and complete the `responsible-data-checklist`. Keep credentials and raw sensitive data out of the repository entirely.<!-- /IF -->
