{
  "skill_name": "data-project",
  "evals": [
    {
      "id": 1,
      "prompt": "I have a CSV of city eviction filings. Can you set up a repo for it?",
      "expected_output": "Infers a low level (L0/L1), states the assumption and offers to climb. After a short interview, scaffolds a SMALL tree (README, AGENTS.md, ROADMAP.md, .gitignore/.gitattributes, data/{raw,interim,processed} with .gitkeep, DATA-DICTIONARY.md, decision-log.md, CHANGELOG.md). Does NOT emit a pipeline, governance, or OKF bundle. ROADMAP.md lists deferred higher-level concerns. No unfilled {{ }} tokens remain.",
      "files": []
    },
    {
      "id": 2,
      "prompt": "Set up a reproducible Python project for analyzing eviction data, with a pipeline that re-runs when the data is refreshed.",
      "expected_output": "Infers L2. Scaffolds the L0/L1 set plus the Python reproducible stack: environment.yml, Snakefile + config.yaml at the repo root (the single-pipeline default; a repo hosting several liberated pipelines would instead give each its own pipelines/<name>/), pyproject.toml, pre-commit config, exploratory/ for notebooks (NOTEBOOKS flag on → nbstripout git filter + AGENTS note). README includes the conda + snakemake run instructions (PIPELINE block kept). Offers to climb to L3 (collaboration) on completion.",
      "files": []
    },
    {
      "id": 3,
      "prompt": "We're starting a multi-partner project with sensitive tenant records across three housing nonprofits. Help me set up the repository.",
      "expected_output": "Infers L3+ and marks sensitivity = sensitive-human and multi-org. Generates the built L3 collaboration/governance set (GOVERNANCE with access tiers + retention + remedy/escalation, CHARTER, collaboration-protocol, ROLES + CODEOWNERS, CONTRIBUTING, CODE_OF_CONDUCT). Fires the affordance-duty coupling: identifier/retention/audit affordances ship with the matching access-tier and contestable-governance language in GOVERNANCE.md. Names design partners vs. beneficiaries separately and flags role gaps. ROADMAP.md holds only genuinely deferred concerns (above L3, or ones the user chose to defer); the responsible-data checklist is recommended before any data ingestion.",
      "files": []
    },
    {
      "id": 4,
      "prompt": "Audit this repository against open, reproducible, and inclusive data standards and tell me what's missing.",
      "expected_output": "Enters Audit mode (read-only). Scores the repo against levels L0-L5 and the six goals, prints a prioritized gap table (Goal/Level -> Found? -> Recommended template/practice -> Source), checks OKF conformance if a knowledge/ bundle exists, and offers to scaffold the missing rungs without modifying the repo until approved.",
      "files": []
    },
    {
      "id": 5,
      "prompt": "We're publishing this eviction dataset and its helper skills so other agents can find and use them. Set up agent discovery.",
      "expected_output": "Infers L5 (publish) and sets the ARD flag because the project exposes agentic resources (nested .skills/ and a published dataset). Generates a repo-scope ai-catalog.json advertising the project's resources (Markdown skills as application/ai-skill+md, the dataset) with domain-anchored URNs (urn:ai:<domain>:skills:<slug>), a host block, capabilities and 2-5 representativeQueries, plus a DISCOVERY.md companion. Pairs it with the OKF knowledge bundle. Defers serving the manifest at the host /.well-known/ai-catalog.json path and registering with an org/ecosystem registry into ROADMAP.md. specVersion is \"1.0\"; ai-catalog.json is valid JSON and no unfilled {{ }} tokens remain.",
      "files": []
    }
  ]
}
